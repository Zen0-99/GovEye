feat(02-04): Room database v2 + 6 cache-first repositories + tests

Room schema (v2, 9 entities):
- MpEntity expanded from 4 to 19 fields (denormalized party/constituency per D-15)
- MpFtsEntity updated FTS columns (nameListAs, nameDisplayAs, constituencyName, partyName)
- DivisionEntity, DivisionVoteEntity (composite PK), BillEntity, BillStageEntity (composite PK)
- HansardContributionEntity, InterestEntity (fieldsJson for dynamic fields), FollowEntity (no TTL)
- Converters.kt: @TypeConverter for List<String> <-> String
- GovEyeDatabase v2 with @TypeConverters, 7 DAO declarations

DAOs (7 total):
- MpDao, DivisionDao, BillDao, HansardDao, InterestDao, FollowDao
- SearchDao updated for expanded FTS columns
- All DAOs: Flow for reads, suspend for writes (D-20)

Repositories (6 cache-first + 1 local-only):
- MembersRepository: 7-day TTL, pull-to-refresh bypass (D-05/D-07)
- VotesRepository: 1-hour TTL for divisions
- BillsRepository: 1-hour TTL for bills
- HansardRepository: 4-hour TTL, runCatching for graceful degradation (D-09)
- InterestsRepository: 30-day TTL
- FollowRepository: local-only, no API, no TTL (D-16)

DatabaseModule:
- 7 DAO providers + 5 mapper providers + 6 repository providers

Tests:
- MembersRepositoryTest (5 tests): FRESH, STALE, EMPTY, refresh bypass, API failure
- VotesRepositoryTest (3 tests): cached, empty, stale
- BillsRepositoryTest (3 tests): cached, empty, stale
- GovEyeDatabaseTest (6 tests): MP CRUD, division+votes, bill+stages, FTS, follow, type converter
- All Robolectric tests use @Config(sdk = [34]) for compatibility

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
