fix(01-02): single Sky color scheme, conditional AMOLED toggle

- Remove color scheme picker from Settings UI — Sky (dynamic) is the
  only scheme now. Backend (AppTheme enum, color scheme classes,
  ThemeViewModel.setAppTheme) remains intact for future re-enabling
- Default app theme changed from CORAL to SKY in ThemePreferences
  and ThemeViewModel
- AMOLED toggle only visible when app is effectively dark (Dark mode
  or System mode with system in dark). Hidden in Light mode and
  System mode with system in light — toggle has no effect when
  app is light

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
