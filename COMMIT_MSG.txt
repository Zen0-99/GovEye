fix(01-02): UAT fixes — crash, splash, theme consolidation, color tuning

- Fix NavKey rememberSaveable crash: store tab as Int index (Bundle-safe)
  instead of NavKey data object which can't serialize to Bundle
- Fix splash screen visibility: add 600ms setKeepOnScreenCondition delay
  so splash is perceptible on fast cold starts
- Consolidate Sky/Monet: remove static SkyColorScheme, rename Monet to Sky
  (dynamic wallpaper-based colors). AppTheme enum reduced 5 to 4 options
- Adjust Forest colors: dark mode less intense (darkened primary/container),
  light mode less bright/saturated (muted container tones)
- Adjust Ember colors: shifted hue toward red from terracotta to avoid
  peach appearance (light) and brown (dark)
- AMOLED toggle always enabled: consistent appearance across all mode
  settings; AMOLED only takes visual effect in dark mode

All 7 UAT items pass human verification on emulator.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
